<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.Eclipse.LoGoYoGo.mapper.LoginMapper">
	<select id="testBaseDTO" parameterType="baseDTO" resultType="baseDTO">
		SELECT 
			id, pw
		FROM logininfo
		WHERE id = #{id}
	</select>
</mapper>
